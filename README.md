This project demonstrates the use of the DBSCAN (Density-Based Spatial Clustering of Applications with Noise) algorithm on the Wine dataset.

The goal is to group similar data points into clusters and identify noise (outliers) without specifying the number of clusters in advance.
